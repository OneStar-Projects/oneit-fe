# Agent部署功能前端实现说明

## 概述

根据后端的Agent部署功能设计文档，前端已经完成了相应的功能实现，支持页面下发和部署agent。

## 主要功能

### 1. Agent包管理
在BuiltinComponent页面中新增了"Agent管理"标签页，包含以下功能：
- **Agent信息配置**：配置Agent类型、版本、二进制下载地址
- **Ansible脚本管理**：编辑部署脚本内容
- **配置模板管理**：管理Agent配置文件模板
- **额外变量配置**：设置部署时的默认变量

### 2. 部署管理
在BuiltinComponent页面中新增了"部署管理"标签页，包含以下功能：
- **主机Agent列表**：显示已部署的Agent状态
- **批量部署**：选择多个主机进行Agent部署
- **部署状态跟踪**：实时显示部署进度和状态
- **部署详情查看**：查看部署配置和错误信息

### 3. 待部署主机管理
将原有的ManagedHost重新定位为"待部署主机管理"，包含以下功能：
- **主机信息管理**：添加、编辑、删除待部署主机
- **SSH连接配置**：配置SSH连接信息和认证方式
- **SSH连接测试**：测试SSH连接是否正常
- **批量导入**：支持CSV文件批量导入主机

## 使用流程

### 1. 配置Agent模板
1. 进入"模板中心"页面
2. 选择或创建一个BuiltinComponent
3. 点击"Agent管理"标签页
4. 配置Agent信息、Ansible脚本、配置模板等
5. 保存配置

### 2. 添加待部署主机
1. 进入"待部署主机管理"页面
2. 点击"添加"按钮
3. 填写主机标识、SSH IP、用户名、认证方式等信息
4. 测试SSH连接
5. 保存主机信息

### 3. 执行Agent部署
1. 回到"模板中心"页面
2. 选择已配置的BuiltinComponent
3. 点击"部署管理"标签页
4. 点击"部署到主机"按钮
5. 选择要部署的主机
6. 确认部署，系统将自动执行Ansible脚本

## 技术实现

### 1. 数据模型扩展
- 扩展了`Component`接口，添加了Agent部署相关字段
- 新增了`HostAgent`接口，管理主机上的Agent部署记录
- 更新了`IManagedHost`接口，重新定位为待部署主机

### 2. API接口
- 新增了HostAgent相关的CRUD API
- 新增了部署执行和状态查询API
- 更新了ManagedHost相关的API

### 3. 组件实现
- `AgentManagement`：Agent配置管理组件
- `DeploymentManagement`：部署管理组件
- 更新了`ManagedHostsTable`和`ManagedHostModal`组件

### 4. 国际化
- 添加了中文翻译支持
- 更新了相关的翻译文件

## 注意事项

1. **权限控制**：确保用户有相应的权限才能执行部署操作
2. **SSH安全**：请确保SSH凭据的安全性和正确性
3. **网络连接**：确保目标主机能够访问Agent二进制下载地址
4. **Ansible环境**：后端需要正确配置Ansible环境

## 后续优化

1. **部署脚本测试**：实现Ansible脚本语法检查和测试功能
2. **部署历史**：添加更详细的部署历史记录
3. **批量操作**：支持批量删除、批量重试等操作
4. **监控集成**：与现有的监控系统更好地集成
